# Variáveis de ambiente para deploy no Render (homologação)
# Copie as variáveis do PostgreSQL criado no Render e configure no painel

# Banco de dados (Render PostgreSQL - use Internal Database URL ou variáveis individuais)
DB_HOST=dpg-xxx.oregon-postgres.render.com
DB_PORT=5432
DB_NAME=aquidolado_xxx
DB_USER=aquidolado_xxx
DB_PASSWORD=xxx

# JWT - OBRIGATÓRIO alterar (mínimo 256 bits / 32 caracteres)
JWT_SECRET=sua-chave-secreta-super-segura-minimo-32-caracteres

# CORS - URL do frontend na Vercel (ex.: https://aquidolado-xxxx.vercel.app)
CORS_ALLOWED_ORIGINS=https://aquidolado.vercel.app

# Profile Spring Boot (necessário para CORS e configurações de prod)
SPRING_PROFILES_ACTIVE=prod

# Porta (Render define PORT automaticamente)
PORT=8080
