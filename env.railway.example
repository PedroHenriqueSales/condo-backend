# Variáveis de ambiente para deploy no Railway
# Copie para .env ou configure no painel do Railway

# Banco de dados (Railway fornece PostgreSQL com variáveis pré-configuradas)
DB_HOST=containers-us-west-xxx.railway.app
DB_PORT=5432
DB_NAME=railway
DB_USER=postgres
DB_PASSWORD=xxx

# JWT - OBRIGATÓRIO alterar em produção (mínimo 256 bits / 32 caracteres)
JWT_SECRET=sua-chave-secreta-super-segura-minimo-32-caracteres

# Opcional: expiração do token em ms (padrão: 86400000 = 24h)
JWT_EXPIRATION_MS=86400000

# CORS (se o frontend estiver em domínio separado)
# Ex.: https://app.aquidolado.com
CORS_ALLOWED_ORIGINS=https://app.aquidolado.com

# Profile Spring Boot (homologação/produção)
SPRING_PROFILES_ACTIVE=prod

# Porta (Railway define PORT automaticamente)
PORT=8080
